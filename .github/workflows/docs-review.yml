name: Docs review

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]

env:
  DOTENV_PRIVATE_KEY_CI: ${{ secrets.DOTENV_PRIVATE_KEY_CI }}

jobs:
  docs-review:
    if: github.event.pull_request.draft == false
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    timeout-minutes: 15
    steps:
      - name: Checkout PR head
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          fetch-depth: 0

      - name: Install bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dotenvx
        run: curl -fsS https://dotenvx.sh | sh

      - name: Install Codex CLI
        run: |
          mkdir -p "$HOME/.local/bin"
          cat > "$HOME/.local/bin/codex" <<'EOF'
          #!/usr/bin/env bash
          exec bunx --yes @openai/codex@0.117.0 "$@"
          EOF
          chmod +x "$HOME/.local/bin/codex"
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"

      - name: Configure Codex auth
        run: dotenvx run -f .env.ci -- ./infra/scripts/setup-codex-auth.sh

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Run docs review
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO: ${{ github.repository }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha }}
          REPO_ROOT: ${{ github.workspace }}
        run: dotenvx run -f .env.ci -- bun run workflows/docs-review/src/main.ts
