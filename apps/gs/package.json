{
  "name": "@redc/git-server",
  "private": true,
  "type": "module",
  "scripts": {
    "manual": "bun run src/manual/cli.ts",
    "test": "bun test",
    "test:integration": "bun test ./src/tests/*.integration.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
