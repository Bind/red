{
  "name": "@redc/git-server",
  "private": true,
  "type": "module",
  "scripts": {
    "manual": "bun run src/manual/cli.ts",
    "test": "bun test",
    "test:integration": "sh -lc 'GIT_SERVER_RUN_INTEGRATION=1 bun test src/tests/integration.test.ts && GIT_SERVER_RUN_INTEGRATION=1 bun test src/tests/auth-integration.test.ts'"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
