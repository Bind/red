{
  "name": "redc",
  "version": "0.1.0",
  "type": "module",
  "workspaces": ["apps/*", "pkg/*"],
  "bin": {
    "redc": "apps/ctl/cli/index.ts"
  },
  "scripts": {
    "dev": "bun run --watch apps/ctl/index.ts",
    "start": "bun run apps/ctl/index.ts",
    "cli": "bun run apps/ctl/cli/index.ts",
    "claw:manual": "bun run apps/ctl/claw/manual.ts",
    "test": "bun test",
    "deploy": "./infra/scripts/deploy.sh"
  },
  "dependencies": {
    "hono": "^4.7.0",
    "yaml": "^2.7.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "sst": "^3"
  }
}
