node_modules/
# Runtime plaintext .env — produced by `dotenvx decrypt` or tooling, never committed.
.env
# Per-developer local overrides
.env.local
.env.*.local
# dotenvx private keys — never committed; stored on each box / in CI secret instead.
.env.keys
# Note: .env.ci / .env.production / .env.preview / .env.development ARE committed
# (they hold dotenvx-encrypted ciphertext, safe to track).
*.db
*.db-wal
*.db-shm
.sst/
apps/web/dist/
.claw-artifacts/
.bun-tmp/
.wide-events-agent/
.codex-artifacts/
.codex-debug/
.zig-cache/
zig-cache/
experiments/bash-runtime-lab/.tmp-compose-data/
experiments/bash-runtime-lab/data/
